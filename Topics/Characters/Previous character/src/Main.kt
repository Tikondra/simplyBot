const val ONE = 1

fun main() {
    val a: Char = readln().first()
    val b: Char = readln().first()
    val c: Char = readln().first()
    val d: Char = readln().first()

    println(a - ONE)
    println(b - ONE)
    println(c - ONE)
    println(d - ONE)
}