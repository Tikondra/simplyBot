import java.util.Scanner

fun main() {
    val scanner = Scanner(System.`in`)
    val one = scanner.nextLine()
    val two = scanner.nextLine()
    val tree = scanner.nextInt()
    val four = scanner.nextInt()
    println(one)
    println(two)
    println(tree)
    println(four)
}